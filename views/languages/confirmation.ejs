<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Translator</title>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="public/assets">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Barcode+39&display=swap" rel="stylesheet">
    <script defer src="server.js"></script> 
    <title>Translator Page</title>
    </head>

    <style>
 body {
    font-family: 'gloria hallelujah', cursive;
    color: #333;
    margin: 10px;
    padding: 0;
}

.logo {
    font-size: 24px;
}

.menu i {
    font-size: 24px;
    cursor: pointer;
}

#translator-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
}

.language-selector {
    position: relative;
    display: inline-block;
    margin: 10px;
}

.language-selector select {
    padding: 10px;
    border-radius: 20px;
    border: 2px solid #ccc;
    outline: none;
}

.language-selector i {
    position: absolute;
    right: 10px;
    top: 14px;
    pointer-events: none;
}

#input-text, #output-text {
    width: 80%;
    margin: 10px 0;
    padding: 15px;
    border: none;
    border-radius: 20px;
    box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    font-size: 16px;
}

#confirm {
    font-family: 'libre barcode 39', cursive;
    width: 80%;
    margin: 10px 0;
}

#translate-button {
    font-family: 'Gloria Hallelujah', cursive;
    margin: 20px;
    padding: 10px 20px;
    background-color: #4CAF50;
    color: white;
    border: none;
    border-radius: 20px;
    cursor: pointer;
    font-size: 16px;
    display: flex;
    align-items: center;
}

#translate-button i {
    margin-right: 10px;
}

#features {
    display: flex;
    overflow-x: auto;
    padding: 20px;
    white-space: nowrap;
}

.feature-card {
    display: inline-block;
    width: 150px;
    margin-right: 20px;
    background-color: white;
    border: 1px solid #ccc;
    padding: 10px;
    border-radius: 10px;
    text-align: center;
}

.confirm {
    margin: 10px;
    text-align: left;
    text-indent: 0;

}

footer {
    background-color: #343a40;
    color: white;
    text-align: center;
    padding: 10px 20px;
    position: fixed;
    bottom: 0;
    width: 100%;
}

nav {
    position: relative;
    grid-column: 1 / -1; /* Spans across all columns */
    background-color: #f0f0f0; /* Light background */
    padding: 10px; /* Padding for the nav bar */
    display: flex; /* Flex layout */
    justify-content: space-between; /* Evenly spaced items */
    border: 10px solid transparent; /* Create a border with the same width as the image border */
    border-image: url('/assets/rb.png') 30 round; /* Replace with your image path */
    border-image-width: 7px; 
}


    </style>

<nav>
    <a href="/">Home</a>
    <br>
    <a href="/languages/index">Languages</a>
    <br>
    <a href="/languages/new">Create a new language</a>
</nav>


<body>

    <h2>Click the button below to copy the <%= language.name %> code.</h2>

    <button onclick="copyText()">Copy Text</button>

<script>
function copyText() {
    /* Get the text field */
    var copyText = document.getElementById("confirm");

    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */

    /* Copy the text inside the text field */
    document.execCommand("copy");

    /* Alert the copied text */
    alert("Copied the text: ");
}
</script>

    <form action="/memory" method="POST">
        <textarea rows="6" name="user" id="confirm" placeholder="Enter text..."> 
        
        Create a language called <%= language.name %> using <%= language.agent %> as my agent. The language will be used for a <%= language.purpose %> project about a(n) <%= language.context %>. It will use  <%= language.consonant %> consonants and <%= language.vowel %> vowels based on <%= language.grammar %>.
        
    </textarea>
    <button id="translate-button">Confirm</button>
        
</form>
      


    <footer>
        © 2024 The Lingularity from Martelligence. All rights reserved.
    </footer>

</body>
</html>